# Monsieur-Train

## How to use:
    - rename `rename.env` to `.env`
    - fill discord bot token
    - Install requirements with `pip install -r requirements.txt`
    - run `main.py`
    - and you're bot should be up